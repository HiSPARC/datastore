# Copy this file into the parent directory and make sure it can be
# imported by the wsgi application.
DATA_ROOT_DIR ="/tmp/filesystem"
DATAFILE_CLUSTERS = DATA_ROOT_DIR + "/clusters.h5"
CSV_CLUSTERS = "/tmp/clusters.csv"
